# Kaggle execution dependencies (Phase 11 attention-only QAT).
#
# The torch stack IS pinned here on purpose. The Kaggle image at submit time
# shipped torch 2.10.0+cu128 / torchvision 0.25.0+cu128 / torchaudio 2.10.0+cu128
# (CUDA 12.8), but our dependency set pulls torch 2.14.0. Installing torch alone
# left torchvision mismatched and transformers crashed at import with
# "RuntimeError: operator torchvision::nms does not exist". We therefore pin the
# matching CUDA 13 pair explicitly (the validated working stack):
#   torch 2.14.0+cu130, torchvision 0.29.0+cu130, CUDA 13.0.
# kaggle/kernel.py additionally removes the stale torchaudio (unused here).
torch==2.14.0
torchvision==0.29.0
transformers==5.17.0
datasets==5.0.1
evaluate==0.4.6
jiwer==4.0.0
accelerate==1.15.0
numpy==2.5.3
soundfile==0.14.0
pyyaml==6.0.3
psutil==7.2.2
torchcodec==0.16.0
safetensors>=0.4
