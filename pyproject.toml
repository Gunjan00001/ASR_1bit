[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "onebit-asr"
version = "1.3.0"
description = "One-bit Wav2Vec2-Conformer ASR experiments"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
