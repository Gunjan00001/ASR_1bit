# Pinned 2026-09-12: tested on Python 3.14.3, Windows 11, CPU-only (torch +cpu build).
torch==2.14.0
torchaudio==2.11.0
transformers==5.17.0
datasets==5.0.1
evaluate==0.4.6
jiwer==4.0.0
accelerate==1.15.0
numpy==2.5.3
soundfile==0.14.0
pyyaml==6.0.3
pytest==9.1.1
psutil==7.2.2  # peak-RSS measurement in eval/benchmark scripts
torchcodec==0.16.0  # required by datasets audio decoding (see README for FFmpeg DLL setup)
